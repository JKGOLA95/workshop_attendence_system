services:
  - type: web
    name: workshop-backend
    rootDir: backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn workshop_backend:app --host 0.0.0.0 --port 8000
